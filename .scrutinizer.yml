build:
  environment:
    node: v8.14.0
    mongodb: true
  nodes:
    coverage:
      tests:
        override:
          - command: npm test
            coverage:
              file: coverage/clover.xml
              format: clover